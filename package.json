{
  "name": "cleanin-extension",
  "version": "1.3.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build:dev": "CLEANIN_EXTENSION_TARGET=dev vite build --mode development",
    "build:prod": "CLEANIN_EXTENSION_TARGET=prod vite build --mode production",
    "build:all": "npm run build:dev && npm run build:prod",
    "package:prod": "npm run build:prod && node scripts/package_release.mjs",
    "validate": "python3 -m json.tool dist/manifest.json >/dev/null && node --check dist/content.js && node --check dist/background.js && node --check dist/sidepanel/sidepanel.js && python3 -m py_compile scripts/render_extension_icons.py && git diff --check"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "typescript": "^5.4.5",
    "vite": "^5.2.12"
  }
}
